

<nav class="bg-gray-900  text-gray-200 fixed top-0 w-full z-50">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
      <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
          <img src="/logo.png" class="h-12" alt="Flowbite Logo" style="background-repeat: no-repeat;" />
          <span class="self-center text-2xl font-semibold whitespace-nowrap ">FREEDOM</span>
      </a>



    <div class="items-center justify-between  w-full md:flex md:w-auto md:order-1" id="navbar-user">

      <input type="text" id="inputSearch" class="rounded-full md:w-72 w-full md:mr-6 border m-auto md:m-0   mt-5 md:mt-0 bg-gray-100 px-4 py-1  text-gray-600 focus:outline-none " placeholder="pesquisar...">
      
      <ul class="flex  font-medium px-4 py-2 md:p-0 mt-4  bg-gray-600 rounded-lg  md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:bg-transparent justify-around  ">
        
        
        
        <li>
          <a href="/" class="block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0 " >Home</a>
        </li>
        <li>
          <a href="/filmes/page/1" class="block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0 ">Filmes</a>
        </li>
        <li>
          <a href="/series/page/1" class="block py-2 px-3 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0 ">Series</a>
        </li>

      </ul>
    </div>
    </div>
  </nav>
  

  <script>
    document.getElementById('inputSearch').addEventListener('keyup', (event)=>{
      const inputSearch = document.getElementById('inputSearch')

      if(event.key === 'Enter' && !event.shiftKey){
        if(inputSearch.value.length > 0){
          window.location.href = `/search/${inputSearch.value}`
        }else{
          alert('Digite algo para pesquisar!')
        }

      }

    })

    // document.getElementById('inputSearch').addEventListener('keyup', (event)=>{
    //   const inputSearch = document.getElementById('inputSearch')

    //   if(event.key === 'Enter' && !event.shiftKey){
    //     if(inputSearch.value.length > 0){
    //       fetch('/search', {
    //         method:'POST', 
    //         headers: {'Content-Type': 'application/json'},
    //         body: JSON.stringify({
    //           arguments: inputSearch.value
    //         })
    //       })
    //       .then(res => res.json())
    //       .then(res =>{
    //         console.log(res)
    //       })
    //     }else{
    //       alert('Digite algo para pesquisar!')
    //     }

    //   }

    // })
  </script>